class Photo:
    def __init__(self, id, title, url, thumbnailUrl, albumId):
        self.id = id
        self.title = title
        self.url = url
        self.thumbnailUrl = thumbnailUrl
        self.albumId = albumId
